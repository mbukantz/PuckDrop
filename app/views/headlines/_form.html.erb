
    <div class = "main-body">
      <%= form_for [@league,@headline] do |f| %>
        <% if @headline.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@headline.errors.count, "error") %> prohibited this video from being saved:</h2>
            <ul>
            <% @headline.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= f.hidden_field :league_id, value: @league.id %><br>
        </div>

        <div class="field">
          <%= f.label :name, "Headline" %><br>
          <%= f.text_field :name %>
        </div>
        <div class="field">
          <%= f.label :body, "Article Body" %><br>
          <%= f.text_area :body %>
        </div>
        <div class="field">
          <%= f.label :picture, "Picture Path"%><br>
          <%= f.text_field :picture %>
        </div>
        <div class="actions">
          <%= f.submit class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
